Version Control System
Track Changes
Collaberate Efficiently
Backup and Recovery
Multiple branches and Merging

Github
---------------
Centralized Repository
Collaboration Tools
Backup and Deployment
Code Hosting
collaboration
Community and Open source
Globally share the code


Git code cheet sheet
---------------------------
1)git config --global user.name "shrinidhiNK01"  //username                        | ==>Git and Github is connected
2)git config --global user.email "kulkarnishrinidhi0001@gmail.com" (primary email) |
3)git config --list
4)git init 
5)touch first.html
6)git add .
7)git status
8)git commit -m "add file"
9)git branch -M main
10)git branch 
11)git remote add origin <url>
12)git push -u origin master
13)rm -rf .git :- remove the initialisation file



                                    Workflow
========================================================================================
(Current working)     (Temp storage)          (Permanant Local Storage)
workdir ------------> Staging Area ---------------> Local Repo ------------------> Remote Repository
         (git add .)                (git commit)                  (Git push)       (Github, Gitlab)

GitIgnore:-
=======================
step1:- Make a folder .gitignore
step2:- add the file names u dont want to track  (app.js)
a)track any files except ".css" extention :-  *.css :- write this inside ur .gitignore file
b)dont track this folder :- Folder_name\ :- write this inside ur .gitignore file 


GitRestore:-
==========================
Go back to the last commited repo, Just like doing Undo in Github Repo 
Restoring from staging area (only add . can be reverted)

git restore index.js
or
git restore .

git restore --staged hello.html  #now it wont track this file
or
git restore --staged . 

Git revert:- Comeback from commited file
============================

git revert <Hash Value>    /*-- ':qa' to exit from trap --*/  


Hash Value of a Commited File:-
================================
git log  /*-- 'q' to exit from trap --*/  
git log --oneline

    
Reset Examples:
=========================================
a) Commit → Staging Area
git reset --soft HEAD~1

b) Commit → Unstaged Changes (Working Directory but not staged)
git reset --mixed HEAD~1

c) Commit → Completely Removed (Working Directory also reset)
git reset --hard HEAD~1

Global and Local aliasis:
=========================================
ls -a   //all files in that folder 
nano <file_name>  //text editor  ==>| [alias]           | ==>ctrl+s and ctrl+x to save and exit
                                    |       st=status   |
*> we aliased the status as st , now instead of git status , st will also run

eg:- git config --global alias.st status
eg:- git config --global alias.b branch 

Branches:- For team work
=========================================
git branch
rename:- git branch -M main
new branch:- git branch second
switch:- git switch second
    //or
git checkout -b second
    //or
git switch -c second
delete:- git branch -d second  //deletes only after switching it back to master 

step1:- git checkout -b new-branch
step2:- new file, write in it, save
step3:- git add .
step4:- git commit -m"add all"
step5:- git push -u origin master 
step6:- git checkout master  //now it will go
step7:- git checkout new-branch  //now it will come

//This dont affect one branch projects to another branch projects
//always add and commit , then only it will work 

//To merge files => we have to comback to master(original) branch and merge

git merge second   ,to come out of merge commit editor :-esc ,:qa! and enter

git amend:-
================
//change something in file
git add .
git commit --ammend
or
git commit --ammend -m "Done by me"

git clone:-
==============
git clone <url>
git clone <url> .
git clone --branch <Name> <url>


git pull:-
=====================
//instead of clonong every time, bringing the repo directly to the working Directory
git fetch origin     :- Download the code pushed by others
git merge origin/main or master 

//and pull is the combination of the above fetch and merge, we can directly use pull 
git pull origin master

git difference:-
=======================
//To check the difference
git diff
git diff --staged 
git diff origin/master 

git log --oneline  ==> It will give a hash Value
git diff <hash value1> <hash value2>

git diff --name-only
git diff --cached
git diff --color-words


skip staging area:-
=====================
git commit -a -m "add all"
or
git commit .\index.html -m "add all"
git push 

Git stash:-
=================
It is used to check the code in other branch without committing

step1:- git stash  //temporary area
     :- git stash list
step2:- git checkout <branch_name> //branch switch
step3:- git checkout <previous_branch_name>  //back to previous branch
step4:- git stash pop  //to get the code back and it will be removed from stash area
come from stach area to current working directory:- git stash apply 
                                       Perticular:- git stash apply stash@{0}
                                       Perticular:- git stash pop stash@{0}

//To remove the stash area
git stash drop
git stash clear

Collaberate with other team members in Github Repo:-
====================================================
step1:- git pull origin master
step2:- git checkout -b <branch_name>
step3:- new file, write in it, save
step4:- git add .
step5:- git commit -m"add all"
step6:- git push -u origin master 
step7:- git checkout master  //now it will go
step8:- git checkout new-branch  //now it will come

or
Team lead:-
===================
github->repo making-> Invite collaborator->enter email id->add people->type username of that other team member->add->Invitation in Github of other team collaborator->accept Invitation->now team lead will push his code to the repo, that can be used by all his added collaborators

Team member:-
=================== 
1)git clone of that pushed repo 
2)then add his files
3)before pushing his files =>git config --global --unset user.name "username" and git config --global --unset user.email "email_id" of that team member 
4)then see the git config --list 
5)go to search bar and type "credential manager" and open it
6)click on windows credential
7)click on git:https://github.com
8)click on remove
9)now push the code ->Authenticate

Team Lead:-
==================
git pull origin master